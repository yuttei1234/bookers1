<h1>
  <Strong>Editing Book</Strong>
</h1>

<% if @book.errors.any? %>
  <%= @book.errors.count %> errors prohibited this book from being saved:
  <%= @book.errors.full_messages.each do |message| %>
    <%= message %>
  <% end %>
<% end %>

<%= form_with model: @book,local true do |f| %>
  <h2>title</h2>
  <%= f.text_field :title %>

  <h2>body</h2>
  <%= f.text_area :body %>
  <%= f.sbumit 'Update Book' %>

  <%= link_to "Show", book_blog_path(@book.id) %>
  <%= link_to "Back", books_blog_path %>

  <% end %>